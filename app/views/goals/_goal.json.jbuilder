json.extract! goal, :id, :name, :pageview, :newuser, :bouncerate, :achieved_pageview, :achieved_newuser, :achieved_bouncerate, :project_id, :requirement_id, :created_at, :updated_at
json.url goal_url(goal, format: :json)
